# Demo-версия CRM Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Поднять публичный demo-инстанс CRM на `daily-rental-crm.itartyom.space`, изолированный от прода по БД/медиа/портам, с заходом через выбор роли (`/dev_auth`) и фиктивными данными.

**Architecture:** Demo живёт в отдельном клоне репо `/opt/fil-crm-demo` на том же сервере. Код общий с продом, режим определяется флагами: `FIL_DEV_AUTH_ONLY=1` на бэке (отключает `/auth/login`), `VITE_DEMO=1` на фронте (редирект на `/dev_auth` вместо `/login`, баннер «DEMO», поясняющий текст). Стартовый скрипт `demo_start.sh` создаёт tmux-сессию `fil-crm-demo` с бэком на порту 8001 (worker не запускаем). Сид-данные — обезличенный snapshot прода в `docs/demo-seed.sqlite3`, готовится один раз скриптом `scripts/demo_make_seed.py`.

**Tech Stack:** FastAPI / SvelteKit / SQLite (raw SQL) / tmux / nginx / vite. Скрипты на python через `uv run`.

---

## Файлы, которые добавим/изменим

**Новые файлы:**
- `demo_start.sh` — стартовый bash-скрипт demo-инстанса.
- `.env.demo.example` — шаблон env для demo (коммитим только example).
- `frontend/src/lib/demo.js` — единая точка чтения `VITE_DEMO`-флага, чтобы не разводить хардкод по компонентам.
- `frontend/src/lib/ui/DemoBadge.svelte` — компонент-бейдж «DEMO», рендерится когда флаг активен.
- `scripts/demo_make_seed.py` — генератор обезличенного фикстура из прод-дампа (запускается один раз локально).
- `scripts/demo_reset.py` — ручной сброс demo-БД и demo-media к фикстуру.
- `docs/nginx-daily-rental-crm.conf` — образец nginx-конфига для нового домена.
- `docs/demo-seed.sqlite3` — бинарный фикстур (генерится на финальном шаге, коммитится).
- `docs/demo-media/` — папка с обезличенными обложками квартир (генерится с фикстуром).

**Модифицируемые файлы:**
- `backend/main.py` — `FIL_DEV_AUTH_ONLY` отключает include `auth_login_routes`.
- `frontend/package.json` — новый script `build:demo`.
- `frontend/src/routes/+layout.svelte` — guard редиректит на `/dev_auth` при `VITE_DEMO`, рендерит `DemoBadge`.
- `frontend/src/routes/login/+page.svelte` — при `VITE_DEMO` сразу `goto('/dev_auth')` в `onMount`.
- `frontend/src/routes/dev_auth/+page.svelte` — поясняющий абзац под заголовком при `VITE_DEMO`.

**Каждое изменение — отдельная задача с TDD-where-applicable, отдельным коммитом.**

---

### Task 1: Бэкенд: флаг `FIL_DEV_AUTH_ONLY`

**Files:**
- Modify: `backend/main.py:42-58` (блок include роутов)
- Test: `tests/test_dev_auth_only.py`

- [ ] **Step 1: Написать падающий тест**

Создать `tests/test_dev_auth_only.py`:

```python
import importlib
import os

import pytest
from fastapi.testclient import TestClient


def _build_app(env: dict[str, str]):
    """Перезагружает backend.main с заданным окружением и возвращает app."""
    for key in ("DEBUG", "FIL_DEV_AUTH_ONLY"):
        os.environ.pop(key, None)
    os.environ.update(env)
    import backend.main as main
    importlib.reload(main)
    return main.app


def test_auth_login_present_by_default(tmp_path, monkeypatch):
    monkeypatch.setenv("FIL_DB_PATH", str(tmp_path / "db.sqlite3"))
    app = _build_app({})
    client = TestClient(app)
    # Нет такого юзера -> 401, а не 404 — значит роут существует.
    r = client.post("/auth/login", json={"username": "x", "password": "y"})
    assert r.status_code == 401


def test_auth_login_disabled_when_dev_auth_only(tmp_path, monkeypatch):
    monkeypatch.setenv("FIL_DB_PATH", str(tmp_path / "db.sqlite3"))
    app = _build_app({"FIL_DEV_AUTH_ONLY": "1"})
    client = TestClient(app)
    # Роут не подключён вовсе -> 404.
    r = client.post("/auth/login", json={"username": "x", "password": "y"})
    assert r.status_code == 404


def test_dev_auth_present_when_debug(tmp_path, monkeypatch):
    monkeypatch.setenv("FIL_DB_PATH", str(tmp_path / "db.sqlite3"))
    app = _build_app({"DEBUG": "1", "FIL_DEV_AUTH_ONLY": "1"})
    client = TestClient(app)
    r = client.get("/dev_auth/users")
    # Список юзеров (пусть пустой) — роут включён.
    assert r.status_code == 200
```

- [ ] **Step 2: Прогнать — должны провалиться**

Run: `uv run --env-file .env pytest tests/test_dev_auth_only.py -v`
Expected: `test_auth_login_disabled_when_dev_auth_only` падает (вместо 404 получаем 401), потому что флаг ещё не реализован.

- [ ] **Step 3: Реализовать флаг в `backend/main.py`**

Заменить блок include-роутов так, чтобы `auth_login_routes` подключался только если флаг **не** выставлен:

```python
if os.environ.get("DEBUG"):
    from backend.routes import dev_auth as dev_auth_routes
    app.include_router(dev_auth_routes.router)

app.include_router(users_routes.router)
app.include_router(apartments_routes.router)
app.include_router(clients_routes.router)
app.include_router(bookings_routes.router)
app.include_router(dashboard_routes.router)
app.include_router(reports_routes.router)
app.include_router(expenses_routes.router)
app.include_router(finance_routes.router)
app.include_router(currency_routes.router)

if not os.environ.get("FIL_DEV_AUTH_ONLY"):
    app.include_router(auth_login_routes.router)
```

(Перенести `app.include_router(auth_login_routes.router)` из общего блока в конец, обернув в `if`.)

- [ ] **Step 4: Прогнать тесты — должны пройти**

Run: `uv run --env-file .env pytest tests/test_dev_auth_only.py -v`
Expected: PASS все три.

- [ ] **Step 5: Коммит**

```bash
git add backend/main.py tests/test_dev_auth_only.py
git commit -m "feat(backend): FIL_DEV_AUTH_ONLY отключает /auth/login для demo-инстанса"
```

---

### Task 2: Фронтенд: модуль `lib/demo.js` и build-script

**Files:**
- Create: `frontend/src/lib/demo.js`
- Modify: `frontend/package.json:6-11`

- [ ] **Step 1: Создать `frontend/src/lib/demo.js`**

```javascript
// Единая точка чтения флага demo-сборки. Включается через VITE_DEMO=1
// при `npm run build:demo`. В обычной сборке/dev — false.
export const DEMO = Boolean(import.meta.env.VITE_DEMO);
```

- [ ] **Step 2: Добавить script в `frontend/package.json`**

Заменить блок `"scripts"` на:

```json
"scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "build:demo": "VITE_DEMO=1 vite build",
    "preview": "vite preview",
    "prepare": "svelte-kit sync || echo ''"
},
```

- [ ] **Step 3: Проверить что обычная сборка ещё собирается**

Run: `cd frontend && npm run build`
Expected: сборка завершается без ошибок (`build/` обновляется).

- [ ] **Step 4: Проверить демо-сборку**

Run: `cd frontend && npm run build:demo`
Expected: сборка завершается без ошибок.

- [ ] **Step 5: Коммит**

```bash
git add frontend/src/lib/demo.js frontend/package.json
git commit -m "feat(frontend): VITE_DEMO флаг и npm run build:demo"
```

---

### Task 3: Фронтенд: layout-guard редиректит на `/dev_auth` в demo

**Files:**
- Modify: `frontend/src/routes/+layout.svelte:1-46`

- [ ] **Step 1: Импортировать флаг и подменить редирект**

В блоке `<script>` добавить импорт:

```javascript
import { DEMO } from '$lib/demo.js';
```

Заменить строку с `goto('/login')` (строка 32) на:

```javascript
goto(DEMO ? '/dev_auth' : '/login');
```

И заменить блок «if u && path === '/login'» (строки 35–38) — этот блок всё ещё нужен на проде, но в demo, если человек зайдёт прямо на `/login`, его перебросит сама `/login` (Task 5). Здесь оставляем как есть.

Итоговый фрагмент `$effect`:

```javascript
$effect(() => {
    const path = page.url.pathname;
    const u = getUser();
    if (!u && !isPublic(path)) {
        user = null;
        ready = true;
        goto(DEMO ? '/dev_auth' : '/login');
        return;
    }
    if (u && path === '/login') {
        user = u;
        goto(u.role === 'maid' ? '/cleaning' : '/');
        return;
    }
    if (u && !ratesRefreshed) {
        ratesRefreshed = true;
        refreshRatesIfStale(api);
    }
    user = u;
    ready = true;
});
```

- [ ] **Step 2: Проверить что обычная сборка работает**

Run: `cd frontend && npm run build`
Expected: PASS.

- [ ] **Step 3: Локально проверить дев-сервер**

Run: `cd frontend && npm run dev -- --port 5173` (фон), затем открыть `http://localhost:5173`.
Expected: незалогиненный юзер редиректится на `/login` (без флага `VITE_DEMO` поведение не меняется).

Остановить дев-сервер (`Ctrl+C` в его tmux/окне).

- [ ] **Step 4: Коммит**

```bash
git add frontend/src/routes/+layout.svelte
git commit -m "feat(frontend): layout-guard редиректит на /dev_auth в DEMO режиме"
```

---

### Task 4: Фронтенд: компонент `DemoBadge` + рендер в layout

**Files:**
- Create: `frontend/src/lib/ui/DemoBadge.svelte`
- Modify: `frontend/src/routes/+layout.svelte` (вставка компонента)

- [ ] **Step 1: Создать `frontend/src/lib/ui/DemoBadge.svelte`**

```svelte
<script>
    import { DEMO } from '$lib/demo.js';
</script>

{#if DEMO}
    <div class="demo-badge" aria-label="Demo instance">DEMO</div>
{/if}

<style>
    .demo-badge {
        position: fixed;
        top: 8px;
        right: 8px;
        z-index: 1000;
        background: var(--accent, #c33);
        color: #fff;
        font-family: var(--font-mono, monospace);
        font-size: 10px;
        font-weight: 700;
        letter-spacing: 0.12em;
        padding: 4px 8px;
        border-radius: 3px;
        pointer-events: none;
    }
</style>
```

- [ ] **Step 2: Подключить в `+layout.svelte`**

Добавить импорт рядом с другими (после `import TabBar from '$lib/ui/TabBar.svelte';`):

```javascript
import DemoBadge from '$lib/ui/DemoBadge.svelte';
```

В шаблоне внутри `.app-shell` добавить рендер до `<main>`:

```svelte
{#if ready}
    <div class="app-shell">
        <DemoBadge />
        <main class="app-main">
            {@render children?.()}
        </main>
        {#if showTabs}<TabBar />{/if}
    </div>
{/if}
```

- [ ] **Step 3: Собрать demo-сборку и проверить наличие текста «DEMO» в bundle**

Run: `cd frontend && npm run build:demo && grep -r "DEMO" build/_app/immutable/ | head -3`
Expected: находится хотя бы одна строка с "DEMO" (компонент попал в bundle).

- [ ] **Step 4: Собрать обычную сборку и проверить отсутствие**

Run: `cd frontend && npm run build`
Expected: сборка проходит. Бейдж в обычной сборке существует как dead-code, но `{#if DEMO}` всегда ложно — это нормально, ничего не рендерится.

- [ ] **Step 5: Коммит**

```bash
git add frontend/src/lib/ui/DemoBadge.svelte frontend/src/routes/+layout.svelte
git commit -m "feat(frontend): DemoBadge поверх UI при VITE_DEMO"
```

---

### Task 5: Фронтенд: `/login` редиректит на `/dev_auth` в demo

**Files:**
- Modify: `frontend/src/routes/login/+page.svelte:14-16`

- [ ] **Step 1: Импортировать флаг**

В блоке `<script>` добавить:

```javascript
import { DEMO } from '$lib/demo.js';
```

- [ ] **Step 2: Изменить `onMount`**

Заменить:

```javascript
onMount(async () => {
    devAvailable = await isDevPickerAvailable();
});
```

на:

```javascript
onMount(async () => {
    if (DEMO) {
        goto('/dev_auth');
        return;
    }
    devAvailable = await isDevPickerAvailable();
});
```

- [ ] **Step 3: Сборка проверочно**

Run: `cd frontend && npm run build && cd frontend && npm run build:demo`
Expected: обе проходят.

- [ ] **Step 4: Коммит**

```bash
git add frontend/src/routes/login/+page.svelte
git commit -m "feat(frontend): /login редиректит на /dev_auth в DEMO режиме"
```

---

### Task 6: Фронтенд: пояснение на `/dev_auth` в demo

**Files:**
- Modify: `frontend/src/routes/dev_auth/+page.svelte`

- [ ] **Step 1: Импортировать флаг**

В блоке `<script>` добавить:

```javascript
import { DEMO } from '$lib/demo.js';
```

- [ ] **Step 2: Скрыть кнопку «Назад» в demo и добавить пояснение**

В блоке `<header class="head">` (строки 39–43) заменить разметку на:

```svelte
<header class="head">
    {#if !DEMO}
        <button class="back" onclick={() => goto('/login')} type="button">← Назад</button>
    {/if}
    <h1 class="title">{DEMO ? 'Демо' : 'Dev picker'}</h1>
    <div class="sub">
        {#if DEMO}
            Выбери роль чтобы войти. Изменения видны только тебе и не сохраняются после ресета базы.
        {:else}
            Вход без пароля — только локально
        {/if}
    </div>
</header>
```

- [ ] **Step 3: Сборка проверочно**

Run: `cd frontend && npm run build && cd frontend && npm run build:demo`
Expected: обе проходят.

- [ ] **Step 4: Коммит**

```bash
git add frontend/src/routes/dev_auth/+page.svelte
git commit -m "feat(frontend): пояснение и скрытая кнопка назад на /dev_auth в DEMO"
```

---

### Task 7: `.env.demo.example`

**Files:**
- Create: `.env.demo.example`

- [ ] **Step 1: Создать файл**

```dotenv
# Demo-инстанс CRM. Этот файл — шаблон. На сервере положить как .env.demo
# рядом с demo_start.sh и заполнить настоящими (демо-)значениями.

# Включает роуты /dev_auth/* (выбор роли без пароля).
DEBUG=1

# Отключает обычный /auth/login. Для demo обязательно: единственный путь входа — dev_auth.
FIL_DEV_AUTH_ONLY=1

# Внешние API: для demo не использовать настоящие ключи. Worker мы не запускаем,
# но даже если кто-то ткнёт ручку — секретов нет, никаких реальных интеграций.
TG_API_ID=
TG_API_HASH=
MORELOGIN_TOKEN=
CURRENCY_API_KEY=
```

- [ ] **Step 2: Коммит**

```bash
git add .env.demo.example
git commit -m "chore: .env.demo.example шаблон env-файла для demo-инстанса"
```

---

### Task 8: `demo_start.sh`

**Files:**
- Create: `demo_start.sh`

- [ ] **Step 1: Создать скрипт**

```bash
#!/usr/bin/env bash
# Старт demo-инстанса fil-crm. Полный аналог start.sh, но:
# - tmux session: fil-crm-demo
# - порт: 8001
# - env: .env.demo
# - worker НЕ запускается (никаких внешних дёрганий)
# - при первом запуске копирует фикстур БД и медиа

set -euo pipefail

SESSION=fil-crm-demo
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 1. Если БД нет — копируем фикстур
if [ ! -f "$ROOT/db.sqlite3" ]; then
    if [ ! -f "$ROOT/docs/demo-seed.sqlite3" ]; then
        echo "ERROR: docs/demo-seed.sqlite3 отсутствует. Сначала сгенерируй фикстур (scripts/demo_make_seed.py)." >&2
        exit 1
    fi
    cp "$ROOT/docs/demo-seed.sqlite3" "$ROOT/db.sqlite3"
    echo "Скопирован фикстур в db.sqlite3"
fi

# 2. Если media пуста — копируем demo-media (если они есть)
mkdir -p "$ROOT/backend/media"
if [ -z "$(ls -A "$ROOT/backend/media" 2>/dev/null)" ] && [ -d "$ROOT/docs/demo-media" ]; then
    cp -r "$ROOT/docs/demo-media/." "$ROOT/backend/media/"
    echo "Скопированы demo-media в backend/media"
fi

# 3. Перезапуск tmux
if tmux has-session -t "$SESSION" 2>/dev/null; then
    echo "Сессия '$SESSION' уже существует — перезапускаю."
    tmux kill-session -t "$SESSION"
fi

tmux new-session -d -s "$SESSION" -n backend -c "$ROOT"
tmux send-keys -t "$SESSION:backend" \
    "uv run --env-file .env.demo uvicorn backend.main:app --port 8001" C-m

echo "Demo tmux '$SESSION' запущен: backend :8001 (worker отключён)."
echo "Подключиться: tmux attach -t $SESSION"
```

- [ ] **Step 2: chmod +x и проверить shebang/syntax**

Run: `chmod +x demo_start.sh && bash -n demo_start.sh`
Expected: ничего не выводит (нет синтаксических ошибок).

- [ ] **Step 3: Коммит**

```bash
git add demo_start.sh
git commit -m "feat: demo_start.sh — старт demo-инстанса в tmux fil-crm-demo на :8001"
```

---

### Task 9: Скрипт `scripts/demo_reset.py`

**Files:**
- Create: `scripts/demo_reset.py`

- [ ] **Step 1: Создать скрипт**

```python
"""Ручной сброс demo-БД и demo-media к фикстуру.

Запускать из корня /opt/fil-crm-demo/. Не импортит backend (правило CLAUDE.md).
"""

from __future__ import annotations

import shutil
import sys
from pathlib import Path


def main() -> int:
    root = Path(__file__).resolve().parent.parent
    seed_db = root / "docs" / "demo-seed.sqlite3"
    seed_media = root / "docs" / "demo-media"
    target_db = root / "db.sqlite3"
    target_media = root / "backend" / "media"

    if not seed_db.exists():
        print(f"ERROR: фикстур {seed_db} не найден", file=sys.stderr)
        return 1

    shutil.copy(seed_db, target_db)
    print(f"copied {seed_db} -> {target_db}")

    if target_media.exists():
        shutil.rmtree(target_media)
    target_media.mkdir(parents=True, exist_ok=True)
    if seed_media.exists():
        for item in seed_media.iterdir():
            dst = target_media / item.name
            if item.is_dir():
                shutil.copytree(item, dst)
            else:
                shutil.copy(item, dst)
        print(f"copied {seed_media}/* -> {target_media}/")
    else:
        print(f"warning: {seed_media} отсутствует, медиа не копируем")

    print("demo reset complete. Перезапусти tmux: bash demo_start.sh")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Проверить что скрипт хотя бы парсится**

Run: `uv run python -c "import ast; ast.parse(open('scripts/demo_reset.py').read())"`
Expected: ничего не выводит (валидный python).

- [ ] **Step 3: Коммит**

```bash
git add scripts/demo_reset.py
git commit -m "feat(scripts): demo_reset.py — ручной сброс demo-БД и медиа к фикстуру"
```

---

### Task 10: Скрипт `scripts/demo_make_seed.py`

**Files:**
- Create: `scripts/demo_make_seed.py`

- [ ] **Step 1: Создать скрипт**

Скрипт берёт прод-дамп БД (путь как argv[1]) и пишет обезличенный фикстур в `docs/demo-seed.sqlite3`. Самодостаточный, не импортит backend.

```python
"""Генерирует обезличенный demo-фикстур из дампа прод-БД.

Usage: uv run python scripts/demo_make_seed.py <path-to-prod-dump.sqlite3>

Что делает:
- копирует входной файл в docs/demo-seed.sqlite3
- идёт по списку RULES (таблица.колонка → функция замены) и UPDATE'ит данные
- удаляет sensitive blob-таблицы целиком (если есть)

ВАЖНО: список RULES ниже — стартовый, основан на схеме на момент написания.
Перед запуском ОБЯЗАТЕЛЬНО пройдись по `PRAGMA table_info` каждой таблицы и
дополни RULES любыми полями где встречается имя/телефон/паспорт/документ.
"""

from __future__ import annotations

import shutil
import sqlite3
import sys
from pathlib import Path

# Простой детерминированный пул имён — без faker'а ради одной задачи.
_FIRST = [
    "Алексей", "Мария", "Иван", "Дарья", "Никита", "Анна", "Сергей",
    "Ольга", "Павел", "Екатерина", "Михаил", "София", "Дмитрий", "Юлия",
    "Артур", "Татьяна",
]
_LAST = [
    "Иванов", "Петров", "Сидоров", "Кузнецов", "Смирнов", "Попов",
    "Новиков", "Морозов", "Волков", "Соколов", "Лебедев", "Семёнов",
]


def fake_full_name(seed: int) -> str:
    f = _FIRST[seed % len(_FIRST)]
    l = _LAST[(seed // len(_FIRST)) % len(_LAST)]
    # Согласование рода — простая эвристика по окончанию first name.
    if f.endswith(("а", "я")) and l.endswith(("ов", "ев", "ин")):
        l = l + "а"
    return f"{f} {l}"


def fake_phone(seed: int) -> str:
    return f"+7 999 {(seed // 100) % 1000:03d} {seed % 100:02d}{(seed * 7) % 100:02d}"


def fake_email(seed: int, role: str = "client") -> str:
    return f"{role}{seed}@example.com"


def fake_username(seed: int) -> str:
    return f"user{seed}"


def fake_address(seed: int) -> str:
    streets = ["Ленина", "Дзержинского", "Кирова", "Орджоникидзе", "Ярославского"]
    return f"ул. {streets[seed % len(streets)]}, д. {(seed % 50) + 1}"


# Таблица.колонка → функция (id_or_seed, current_value) -> new_value.
# Если таблицы нет в БД, шаг просто пропускается.
RULES = {
    "users": {
        "full_name": lambda i, _: fake_full_name(i),
        "username": lambda i, _: fake_username(i),
        "password_hash": lambda *_: "",
    },
    "clients": {
        "full_name": lambda i, _: fake_full_name(i + 1000),
        "phone": lambda i, _: fake_phone(i),
        "notes": lambda *_: "",
    },
    "apartments": {
        "address": lambda i, _: fake_address(i),
        "intercom_code": lambda *_: "0000",
        "safe_code": lambda *_: "0000",
        "utility_account": lambda *_: "",
    },
    "bookings": {
        "notes": lambda *_: "",
    },
}

# Таблицы, содержимое которых вырезаем целиком (тяжёлые blob'ы — паспорта-фотки и пр.).
WIPE_TABLES = [
    # Заполнить при реализации, проверив схему. Пример:
    # "client_documents",
    # "passport_scans",
]


def _table_exists(conn: sqlite3.Connection, name: str) -> bool:
    row = conn.execute(
        "SELECT 1 FROM sqlite_master WHERE type='table' AND name=?", (name,)
    ).fetchone()
    return row is not None


def _column_exists(conn: sqlite3.Connection, table: str, col: str) -> bool:
    rows = conn.execute(f"PRAGMA table_info({table})").fetchall()
    return any(r[1] == col for r in rows)


def main(argv: list[str]) -> int:
    if len(argv) != 2:
        print("Usage: demo_make_seed.py <path-to-prod-dump.sqlite3>", file=sys.stderr)
        return 2

    src = Path(argv[1]).expanduser().resolve()
    if not src.exists():
        print(f"ERROR: {src} not found", file=sys.stderr)
        return 1

    root = Path(__file__).resolve().parent.parent
    dst = root / "docs" / "demo-seed.sqlite3"
    dst.parent.mkdir(parents=True, exist_ok=True)
    shutil.copy(src, dst)
    print(f"copied {src} -> {dst}")

    conn = sqlite3.connect(str(dst))
    try:
        # WIPE целиком
        for tbl in WIPE_TABLES:
            if _table_exists(conn, tbl):
                conn.execute(f"DELETE FROM {tbl}")
                print(f"wiped table {tbl}")

        # Применяем RULES построчно по id
        for tbl, cols in RULES.items():
            if not _table_exists(conn, tbl):
                print(f"skip table {tbl} (отсутствует)")
                continue
            valid_cols = {c: fn for c, fn in cols.items() if _column_exists(conn, tbl, c)}
            if not valid_cols:
                continue
            ids = [r[0] for r in conn.execute(f"SELECT id FROM {tbl} ORDER BY id").fetchall()]
            for row_id in ids:
                row = conn.execute(
                    f"SELECT {', '.join(valid_cols.keys())} FROM {tbl} WHERE id=?", (row_id,)
                ).fetchone()
                new_values = []
                for (col, fn), cur in zip(valid_cols.items(), row):
                    new_values.append(fn(row_id, cur))
                set_clause = ", ".join(f"{c}=?" for c in valid_cols.keys())
                conn.execute(
                    f"UPDATE {tbl} SET {set_clause} WHERE id=?",
                    (*new_values, row_id),
                )
            print(f"updated {tbl}: {len(ids)} rows, cols={list(valid_cols)}")

        conn.commit()
    finally:
        conn.close()

    print(f"\nDone. Фикстур: {dst}")
    print("Дальше: глазами проверь sqlite3 docs/demo-seed.sqlite3, и подготовь docs/demo-media/")
    return 0


if __name__ == "__main__":
    raise SystemExit(main(sys.argv))
```

- [ ] **Step 2: Проверить что скрипт парсится**

Run: `uv run python -c "import ast; ast.parse(open('scripts/demo_make_seed.py').read())"`
Expected: ничего не выводит.

- [ ] **Step 3: Smoke-тест на временной пустой БД**

Создать пустую sqlite-«заглушку» и прогнать скрипт, чтобы убедиться что он не падает на отсутствующих таблицах:

Run:
```bash
uv run python -c "import sqlite3; sqlite3.connect('/tmp/empty-prod.sqlite3').execute('CREATE TABLE _ignore(id INTEGER)'); print('ok')"
uv run python scripts/demo_make_seed.py /tmp/empty-prod.sqlite3
```

Expected: «skip table users (отсутствует)», «skip table clients ...», и в конце «Done. Фикстур: docs/demo-seed.sqlite3». Файл `docs/demo-seed.sqlite3` создан.

Удалить созданный фикстур (он будет перезаписан настоящим в Task 12):

Run: `rm docs/demo-seed.sqlite3`

- [ ] **Step 4: Коммит**

```bash
git add scripts/demo_make_seed.py
git commit -m "feat(scripts): demo_make_seed.py — генератор обезличенного фикстура из прод-дампа"
```

---

### Task 11: nginx-конфиг для demo-домена

**Files:**
- Create: `docs/nginx-daily-rental-crm.conf`

- [ ] **Step 1: Создать конфиг**

Скопировать структуру существующего `docs/nginx-sakha-gay.conf`, заменив пути и порт. Файл — пример для деплоя; реальный конфиг кладётся в `/etc/nginx/sites-available/` руками.

```nginx
server {
    server_name daily-rental-crm.itartyom.space;

    client_max_body_size 20m;

    # SvelteKit demo build (npm run build:demo)
    root /opt/fil-crm-demo/frontend/build;
    index index.html;

    # Backend API demo — порт 8001
    location /api/ {
        proxy_pass http://127.0.0.1:8001/;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Demo media (обложки квартир, обезличенные)
    location /media/ {
        alias /opt/fil-crm-demo/backend/media/;
        expires 7d;
        access_log off;
        try_files $uri =404;
    }

    location /_app/immutable/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    listen 443 ssl;
    # Cert выпускается через certbot для daily-rental-crm.itartyom.space:
    # ssl_certificate /etc/letsencrypt/live/daily-rental-crm.itartyom.space/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/daily-rental-crm.itartyom.space/privkey.pem;
    # include /etc/letsencrypt/options-ssl-nginx.conf;
    # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = daily-rental-crm.itartyom.space) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name daily-rental-crm.itartyom.space;
    return 404;
}
```

- [ ] **Step 2: Коммит**

```bash
git add docs/nginx-daily-rental-crm.conf
git commit -m "docs: образец nginx-конфига для daily-rental-crm.itartyom.space"
```

---

### Task 12: Сгенерировать обезличенный фикстур и demo-media

> **Это ручная операция, выполняется один раз с доступом к прод-БД. После — комментарий в коммите фиксирует факт.**

**Files:**
- Create: `docs/demo-seed.sqlite3` (бинарь)
- Create: `docs/demo-media/` (директория с обложками)

- [ ] **Step 1: Снять свежий дамп прод-БД**

Run: `scp cms-gen-bot:/opt/fil-crm/db.sqlite3 /tmp/prod-dump.sqlite3`
Expected: файл скопирован (десятки МБ).

- [ ] **Step 2: Расширить `RULES` и `WIPE_TABLES` в скрипте под актуальную схему**

Открыть `/tmp/prod-dump.sqlite3` через `sqlite3 /tmp/prod-dump.sqlite3 ".schema"`, пройтись по каждой таблице. Для каждой колонки решить: содержит ПД? добавить в RULES. Содержит документ-blob? добавить таблицу в WIPE_TABLES (или конкретное поле в RULES с заменой на пустую строку).

В частности — сейчас (на момент написания плана) актуальные таблицы по миграциям:
- `users` (full_name, username, password_hash) — уже в RULES.
- `clients` (full_name, phone, notes) — уже в RULES.
- `apartments` (title, address, entrance, apt_number, intercom_code, safe_code, utility_account) — `address` и коды уже в RULES; **проверить `title`** — если содержит реальные имена/адреса, добавить в RULES.
- `bookings` (notes) — уже в RULES.
- Таблицы расходов / валют / финансов — обычно без ПД, но проверь.
- Любые таблицы паспортов / документов / приложенных файлов — добавить в WIPE_TABLES.

При необходимости — закоммитить уточнения в `scripts/demo_make_seed.py` отдельным коммитом перед запуском.

- [ ] **Step 3: Запустить генератор**

Run: `uv run python scripts/demo_make_seed.py /tmp/prod-dump.sqlite3`
Expected: создаётся `docs/demo-seed.sqlite3`, в логе видны все обработанные таблицы.

- [ ] **Step 4: Глазами проверить фикстур**

Run: `sqlite3 docs/demo-seed.sqlite3 "SELECT id, full_name, phone FROM clients LIMIT 10; SELECT id, full_name, username FROM users LIMIT 10;"`
Expected: фейковые имена и `+7 999 ...` номера. Никаких остатков прод-данных.

При необходимости — пройтись `sqlite3 docs/demo-seed.sqlite3 ".tables"` и проверить ещё подозрительные таблицы.

- [ ] **Step 5: Подготовить `docs/demo-media/`**

```bash
mkdir -p docs/demo-media
# Скопировать ТОЛЬКО обложки квартир, никаких документов:
scp 'cms-gen-bot:/opt/fil-crm/backend/media/apartments/*' docs/demo-media/apartments/ 2>/dev/null || true
# Если в media есть подкаталоги "passports/" / "documents/" / "telegram/" — НЕ копировать.
```

Глазами просмотреть каждый файл в `docs/demo-media/` — удалить всё, на чём видны лица людей или узнаваемые ориентиры (вывески, номера домов крупным планом).

- [ ] **Step 6: Закоммитить артефакты**

```bash
git add docs/demo-seed.sqlite3 docs/demo-media
git commit -m "chore: обезличенный demo-фикстур и обложки квартир (snapshot $(date +%Y-%m-%d))"
```

> ⚠️ Важно: этот коммит может быть несколько МБ (sqlite + jpeg). Это ожидаемо — обновляться он будет редко (по мере значимых изменений схемы прода или появления новых квартир).

---

### Task 13: Деплой на сервер `cms-gen-bot`

> **Это ручные операции на сервере. Каждый шаг — отдельная проверяемая мелочь. Не коммит, а runbook.**

- [ ] **Step 1: DNS A-запись**

В DNS-панели itartyom.space создать A-запись `daily-rental-crm` → IP `cms-gen-bot`.
Expected: `dig +short daily-rental-crm.itartyom.space` возвращает правильный IP. Может быть задержка распространения.

- [ ] **Step 2: Клон репо**

На сервере:
```bash
ssh cms-gen-bot
sudo git clone /opt/fil-crm /opt/fil-crm-demo
sudo chown -R $(whoami) /opt/fil-crm-demo
cd /opt/fil-crm-demo
git fetch origin && git reset --hard origin/master
```

(Или клонировать из git-remote, как удобнее. Главное — независимая рабочая копия.)

- [ ] **Step 3: Создать `.env.demo`**

```bash
cp .env.demo.example .env.demo
# отредактировать на месте если нужно (обычно — оставить как есть)
```

- [ ] **Step 4: Сборка фронта**

```bash
cd /opt/fil-crm-demo/frontend
npm install
npm run build:demo
```

Expected: `frontend/build/index.html` существует, в bundle есть «DEMO».

- [ ] **Step 5: Nginx + certbot**

```bash
sudo cp /opt/fil-crm-demo/docs/nginx-daily-rental-crm.conf /etc/nginx/sites-available/daily-rental-crm.itartyom.space.conf
sudo ln -s /etc/nginx/sites-available/daily-rental-crm.itartyom.space.conf /etc/nginx/sites-enabled/
sudo nginx -t  # проверить синтаксис
sudo certbot --nginx -d daily-rental-crm.itartyom.space
sudo systemctl reload nginx
```

Expected: `nginx -t` ok; certbot успешно выпустил сертификат и раскомментировал ssl-блок.

- [ ] **Step 6: Запустить demo**

```bash
cd /opt/fil-crm-demo
bash demo_start.sh
```

Expected: в логе «Скопирован фикстур в db.sqlite3», «Demo tmux 'fil-crm-demo' запущен». `tmux ls` показывает обе сессии: `fil-crm` и `fil-crm-demo`.

- [ ] **Step 7: Smoke-test**

```bash
curl -sf https://daily-rental-crm.itartyom.space/api/health
curl -sf https://daily-rental-crm.itartyom.space/api/dev_auth/users | head
curl -sf -X POST https://daily-rental-crm.itartyom.space/api/auth/login \
    -H 'Content-Type: application/json' -d '{"username":"x","password":"y"}' \
    -o /dev/null -w "auth_login_status=%{http_code}\n"
```

Expected:
- `/api/health` → `{"ok":true}`
- `/api/dev_auth/users` → массив фейковых юзеров
- `/api/auth/login` → `auth_login_status=404` (роут отключён через `FIL_DEV_AUTH_ONLY`)

Открыть в браузере `https://daily-rental-crm.itartyom.space/` — должно перебросить на `/dev_auth`, отрисовать список ролей с пояснением, видна плашка «DEMO» в углу.

- [ ] **Step 8: Финальная проверка прода**

Открыть `https://sakha.gay/` — ничего не должно сломаться, никакой плашки «DEMO», обычный `/login`. (Прод-инстанс не пересобирался; но мы меняли `+layout.svelte` — общий артефакт. После следующего обычного `npm run build` на проде поведение должно остаться прежним, проверим что без `VITE_DEMO` редирект всё ещё на `/login`.)

```bash
ssh cms-gen-bot 'cd /opt/fil-crm/frontend && npm run build && sudo systemctl reload nginx'
curl -sI https://sakha.gay/ | head -1
```

Expected: 200 OK, в браузере залогинивается обычно через `/login`.

---

## Self-Review

**Spec coverage:**
- DNS, nginx, отдельный домен → Task 11, 13.
- Отдельный клон `/opt/fil-crm-demo`, tmux `fil-crm-demo`, порт 8001 → Task 8, 13.
- Worker не запускается → Task 8 (`demo_start.sh` без worker).
- `FIL_DEV_AUTH_ONLY` отключает `/auth/login` → Task 1.
- `VITE_DEMO` флаг сборки → Task 2.
- Layout-guard редирект → Task 3.
- DemoBadge → Task 4.
- `/login` → `/dev_auth` редирект → Task 5.
- Пояснение на `/dev_auth` → Task 6.
- `.env.demo.example` → Task 7.
- `scripts/demo_make_seed.py` + контракт обезличивания → Task 10.
- `scripts/demo_reset.py` → Task 9.
- Артефакты (`docs/demo-seed.sqlite3`, `docs/demo-media/`) → Task 12.
- Out of scope: hourly reset, i18n, селектор языка — намеренно не реализованы, что зафиксировано в спеке.

**Placeholder scan:** в плане все шаги содержат конкретный код или конкретные команды. Единственное «реализуй позже» — это `WIPE_TABLES` в Task 10 (пустой список) и `Step 2` в Task 12 (расширь RULES под реальную схему). Это намеренные плейсхолдеры под ручную работу с прод-схемой, и инструкция «как заполнить» дана явно.

**Type/name consistency:** флаг везде называется `FIL_DEV_AUTH_ONLY` (бэк) и `VITE_DEMO` (фронт). Sentinel-модуль `frontend/src/lib/demo.js` экспортирует `DEMO`. Tmux session везде `fil-crm-demo`. Порт везде 8001. БД-фикстур — `docs/demo-seed.sqlite3`. Никаких расхождений.
